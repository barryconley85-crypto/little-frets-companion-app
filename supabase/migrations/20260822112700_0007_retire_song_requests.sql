/*
# Retire learner-to-teacher song requests

Little Frets does not provide a student-to-teacher messaging or request channel.
The retired request table and its historical content are permanently removed so
this pathway cannot be unintentionally re-enabled by a later interface change.
*/

DROP TABLE IF EXISTS public.requests;
